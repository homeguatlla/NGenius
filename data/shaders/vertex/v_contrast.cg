#version 330 core

in vec3 vertexModelspace;
out vec2 textureCoords;

void main(void)
{
	gl_Position = vec4(vertexModelspace.x, vertexModelspace.y, 0.0, 1.0);
	textureCoords = vec2(vertexModelspace.x, vertexModelspace.y) * 0.5 + 0.5;
}