#version 330 core

in vec2 textureCoords;

out vec4 color;
 
uniform sampler2D texture;
const float constrast = 0.3;

void main()
{
	//vec4 textureColor = texture2D(texture, textureCoords).rbga;
	vec4 textureColor = texture2D(texture, textureCoords);
	color = (textureColor - 0.5) * (1.0 + constrast) + 0.5;
}